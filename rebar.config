{port_sources, ["c_src/*.cpp"]}.
{so_name, "pteracuda_nifs.so"}.

{port_envs, [{"(linux|solaris)", "LDFLAGS", "$LDFLAGS c_src/pteracuda_buffer.o -L/usr/local/cuda/lib64 -lstdc++ -lcudart"},
             {"(linux|solaris)", "CXXFLAGS", "$CXXFLAGS -O2 -I/usr/local/cuda/include"}]}.

{port_pre_script, {"make -C c_src", ""}}.
{port_cleanup_script, "make -C c_src clean"}.

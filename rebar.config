{port_sources, ["c_src/*.cpp"]}.
{so_name, "pteracuda_nifs.so"}.

{port_envs, [{"(linux|solaris)", "LDFLAGS", "$LDFLAGS -L/usr/local/cuda/lib64 -lstdc++ -lcudart"},
             {"(linux|solaris)", "CXXFLAGS", "$CXXFLAGS -I/usr/local/cuda/include"}]}.
